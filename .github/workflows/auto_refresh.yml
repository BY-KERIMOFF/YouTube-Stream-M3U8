name: 🕐 Auto Refresh M3U Playlist

on:
  push:
    branches: [ main ]
  schedule:
    # Hər 6 saatda bir işlə
    - cron: '0 */6 * * *'
  workflow_dispatch: # Manual işləmə üçün

jobs:
  refresh-m3u:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Repository checkout
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🐍 Python quraşdır
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Kitabxanaları yüklə
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install yt-dlp --upgrade
        
    - name: 🚀 M3U Playlist yarat
      run: |
        python youtube_auto.py
        
    - name: 📤 Dəyişiklikləri yüklə
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add playlist.m3u status.md youtube_streams.db
        git commit -m "🤖 Avtomatik M3U yeniləmə - $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
        git push.
