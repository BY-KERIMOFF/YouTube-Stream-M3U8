name: YouTube Final M3U Generator

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['github_youtube_final.py', 'turkish.json']

jobs:
  generate:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛠️ Repo yüklə
        uses: actions/checkout@v4
      
      - name: 🐍 Python quraşdır
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Lazımlı kitabxanaları quraşdır
        run: |
          pip install -U yt-dlp requests
          pip install --upgrade pip
      
      - name: 🎥 M3U fayllarını yarat
        run: python github_youtube_final.py
        timeout-minutes: 30
      
      - name: 📊 Nəticə
        run: |
          echo "=== YARADILAN FAYLLAR ==="
          find TR -name "*.m3u8" 2>/dev/null | head -20 || echo "Heç bir fayl yoxdur"
          echo "=== ÜMUMİ SAY ==="
          find TR -name "*.m3u8" 2>/dev/null | wc -l || echo "0"
      
      - name: ✅ Dəyişiklikləri yoxla
        id: check-changes
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add TR/ 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "changes=false" >> $GITHUB_OUTPUT
            echo "🔄 Dəyişiklik yoxdur"
          else
            echo "changes=true" >> $GITHUB_OUTPUT
            echo "📝 Dəyişikliklər var"
            git commit -m "TR: YouTube M3U faylları yeniləndi"
          fi
      
      - name: 🚀 Push et
        if: steps.check-changes.outputs.changes == 'true'
        run: git push
