name: 🚀 SUPER YouTube M3U Update

on:
  schedule:
    # Hər 4 saatda bir yenilə
    - cron: '0 */4 * * *'
  workflow_dispatch: # Manual trigger
  push:
    branches: [ main ]

jobs:
  super-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🛎️ Checkout repo
      uses: actions/checkout@v4
      
    - name: 🐍 Python quraşdır
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Kitabxanaları yüklə
      run: |
        python -m pip install --upgrade pip
        pip install yt-dlp requests
        echo "✅ Kitabxanalar yükləndi"
        
    - name: 🎥 Canlı yayınları kəşf et
      run: |
        echo "🚀 AUTO DISCOVER BAŞLAYIR..."
        python auto_discover.py
        echo "✅ AUTO DISCOVER TAMAMLANDI"
        
    - name: 📝 M3U playlist yarat
      run: |
        echo "📝 M3U GENERATOR BAŞLAYIR..."
        python m3u_generator.py
        echo "✅ M3U GENERATOR TAMAMLANDI"
        
    - name: 📊 Nəticə
      run: |
        echo "📊 SİSTEM NƏTİCƏSİ:"
        echo "=== FAYLLAR ==="
        ls -la public/
        echo "=== M3U STATISTIKA ==="
        if [ -f "public/playlist.m3u" ]; then
          CHANNEL_COUNT=$(grep -c "EXTINF" public/playlist.m3u || true)
          echo "🎯 KANAL SAYISI: $CHANNEL_COUNT"
          echo "=== İLK 10 KANAL ==="
          grep "EXTINF" public/playlist.m3u | head -10
        else
          echo "❌ M3U FAYLI YOXDUR"
          exit 1
        fi
        
    - name: 🚀 Commit et
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "🚀 SUPER YENİLƏMƏ: $(date +'%Y-%m-%d %H:%M') - $(grep -c 'EXTINF' public/playlist.m3u || echo 0) kanal" || echo "⚠️ Yeni dəyişiklik yoxdur"
        git push
