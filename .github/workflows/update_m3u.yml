name: 🎥 AVTOMATİK YouTube M3U Update

on:
  schedule:
    # Hər 6 saatda bir yenilə
    - cron: '0 */6 * * *'
  workflow_dispatch: # Manual trigger
  push:
    branches: [ main ]

jobs:
  auto-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🛎️ Checkout repo
      uses: actions/checkout@v4
      
    - name: 🐍 Python quraşdır
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Kitabxanaları yüklə
      run: |
        python -m pip install --upgrade pip
        pip install yt-dlp requests
        echo "✅ Kitabxanalar yükləndi"
        
    - name: 🔍 Canlı yayınları kəşf et
      run: |
        echo "🔍 Auto Discover başlayır..."
        cd scripts
        python auto_discover.py
        echo "✅ Auto Discover tamamlandı"
        
    - name: 📝 M3U playlist yarat
      run: |
        echo "📝 M3U Generator başlayır..."
        cd scripts
        python m3u_generator.py
        echo "✅ M3U Generator tamamlandı"
        
    - name: 📊 Nəticəni yoxla
      run: |
        echo "📊 Fayllar yoxlanılır..."
        ls -la public/
        if [ -f "public/playlist.m3u" ]; then
          echo "✅ M3U faylı mövcuddur"
          CHANNEL_COUNT=$(grep -c "EXTINF" public/playlist.m3u || true)
          echo "📺 Kanalların sayı: $CHANNEL_COUNT"
        else
          echo "❌ M3U faylı tapılmadı"
          exit 1
        fi
        
    - name: 🚀 Dəyişiklikləri commit et
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "🤖 AVTOMATİK YENİLƏMƏ: $(date +'%Y-%m-%d %H:%M')" || echo "⚠️ Yeni dəyişiklik yoxdur"
        git push
