name: 🎥 AVTOMATİK YouTube M3U Update

on:
  schedule:
    # Hər 6 saatda bir yenilə
    - cron: '0 */6 * * *'
  workflow_dispatch: # Manual trigger
  push:
    branches: [ main ]

jobs:
  auto-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🛎️ Checkout repo
      uses: actions/checkout@v4
      
    - name: 🐍 Python quraşdır
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Kitabxanaları yüklə
      run: |
        python -m pip install --upgrade pip
        pip install yt-dlp requests
        echo "✅ Kitabxanalar yükləndi: yt-dlp, requests"
        
    - name: 🔍 Canlı yayınları kəşf et
      run: |
        echo "🔍 AUTO DISCOVER BAŞLAYIR..."
        python auto_discover.py
        echo "✅ AUTO DISCOVER TAMAMLANDI"
        
    - name: 📝 M3U playlist yarat
      run: |
        echo "📝 M3U GENERATOR BAŞLAYIR..."
        python m3u_generator.py
        echo "✅ M3U GENERATOR TAMAMLANDI"
        
    - name: 📊 Nəticəni yoxla
      run: |
        echo "📊 FAYLLAR YOXLANILIR..."
        echo "=== DATA QOVLUĞU ==="
        ls -la data/ || echo "data/ qovluğu yoxdur"
        echo "=== PUBLIC QOVLUĞU ==="
        ls -la public/ || echo "public/ qovluğu yoxdur"
        
        if [ -f "public/playlist.m3u" ]; then
          echo "✅ M3U FAYLI MÖVCUDDUR"
          CHANNEL_COUNT=$(grep -c "EXTINF" public/playlist.m3u || true)
          echo "📺 KANALLARIN SAYI: $CHANNEL_COUNT"
          echo "📄 M3U FAYLININ İLK SƏTRLƏRİ:"
          head -10 public/playlist.m3u
        else
          echo "❌ M3U FAYLI TAPILMADI"
          exit 1
        fi
        
    - name: 🚀 Dəyişiklikləri commit et
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git status
        git commit -m "🤖 AVTOMATİK YENİLƏMƏ: $(date +'%Y-%m-%d %H:%M')" || echo "⚠️ Yeni dəyişiklik yoxdur"
        git push
